# PingMe - Telegram Reminder Bot

Telegram-бот для создания и управления напоминаниями с использованием aiogram 3.x и FastAPI.

## 🚀 Возможности

- ⏰ Создание напоминаний с указанием времени
- 📝 Управление списком напоминаний
- 🔔 Автоматическая отправка уведомлений
- 💾 Хранение данных в базе данных
- 🌐 REST API для интеграций (FastAPI)

## 🛠 Технологический стек

- **Python 3.10+**
- **aiogram 3.x** - асинхронный фреймворк для Telegram Bot API
- **FastAPI** - современный веб-фреймворк для API
- **SQLAlchemy** - ORM для работы с базой данных
- **SQLite/PostgreSQL** - база данных
- **APScheduler** - планировщик задач для напоминаний

## 📋 Требования

- Python 3.10 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd pingme
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

5. Создайте файл `.env` и добавьте необходимые переменные:
```env
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=sqlite:///./pingme.db
API_HOST=0.0.0.0
API_PORT=8000
```

## 🚀 Запуск

### Запуск бота и API:
```bash
python main.py
```

### Только бот:
```bash
python -m app.bot
```

### Только API:
```bash
uvicorn app.api:app --reload
```

## 📚 Использование

### Команды бота:

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/remind` - Создать новое напоминание
- `/list` - Показать список напоминаний
- `/delete` - Удалить напоминание
- `/cancel` - Отменить текущее действие

### API Endpoints:

- `GET /` - Статус API
- `GET /api/v1/reminders` - Получить список напоминаний
- `POST /api/v1/reminders` - Создать напоминание
- `DELETE /api/v1/reminders/{id}` - Удалить напоминание

## 📁 Структура проекта

```
pingme/
├── app/
│   ├── __init__.py
│   ├── bot/              # Telegram bot logic
│   ├── api/              # FastAPI application
│   ├── database/         # Database models and connection
│   ├── services/         # Business logic
│   └── utils/            # Utility functions
├── .env                  # Environment variables
├── .gitignore
├── main.py               # Application entry point
├── requirements.txt      # Python dependencies
└── README.md
```

## 🤝 Разработка

Проект использует:
- **Black** для форматирования кода
- **Flake8** для линтинга
- **MyPy** для проверки типов

## 📝 Лицензия

MIT License

## 👤 Автор

Ваше имя

## 🐛 Сообщить об ошибке

Если вы нашли ошибку, пожалуйста, создайте issue в репозитории.
